---
sidebar_position: 3
sidebar_label: Using the Web SDK
---

import WebSDK from '/docs/partials/connect/_using_the_sdk.mdx';

# Using the Web SDK

Once you’ve generated a URL, you can pass that to the [web SDK](xxx). This SDK eases the integration between your webapp and the Connect widget’s iframe.

The example on the right is using the [UMD build](xxx). But other options are available.

See the [options reference](xxx) for more details on what options can be passed.

<WebSDK />
